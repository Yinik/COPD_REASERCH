# 核心代码说明文档

本文档对项目中的核心代码文件进行详细说明，帮助评审快速理解系统架构和关键实现。

---

## 一、项目架构总览

```
输入层 (50篇PDF)
    ↓  PDF清洗_v2.py
预处理层 (清洗后文本)
    ↓  数据增强_词条级匹配.py
实体识别层 (126实体种子词典)
    ↓  关系抽取方案_v4_最大覆盖.py
关系抽取层 (675三元组)
    ↓  生成Neo4j导入.py
知识图谱层 (Neo4j图数据库)
    ↓  COPD图谱端到端抽取系统.py
演示层 (端到端系统)
```

---

## 二、核心模块详解

### 2.1 PDF清洗模块

**文件**: `PDF清洗_v2.py`

**功能**: 从PDF医学文献中提取纯文本，去除噪声

**关键技术点**:
- 使用 `pdfplumber` 逐页提取文本，保留排版信息
- **页眉页脚去除**: 通过正则匹配重复出现的页码/期刊名模式识别
- **断行合并**: 检测句末无标点符号（。；！？）的行，与下一行合并
- **参考文献截断**: 识别"参考文献""References"等关键词，截断后续内容

**输入**: `原始文献数据/*.pdf`  
**输出**: `data/清洗后的文本数据/*_cleaned.txt`

**调用示例**:
```python
from PDF清洗_v2 import clean_pdf
text = clean_pdf("input.pdf")
```

---

### 2.2 种子词典构建模块

**文件**: `seed_dict_select.py`, `seed_dict_filter.py`, `seed_dict_pmi_entropy.py`

**功能**: 从清洗文本中提取高质量医学术语，构建种子词典

**处理流程**:
1. **候选术语生成** (`seed_dict_select.py`): 基于N-gram和规则匹配生成候选术语
2. **质量过滤** (`seed_dict_filter.py`): 过滤低频词、停用词、非医学词汇
3. **统计打分** (`seed_dict_pmi_entropy.py`): 计算PMI（点互信息）和熵值，筛选高质量术语

**质量评估指标**:
- **PMI (Pointwise Mutual Information)**: 衡量词语内部凝聚度，值越高越可能是专业术语
- **信息熵**: 衡量词语左右搭配的丰富程度，熵越高越可能是通用词（应过滤）

**输入**: 清洗后文本  
**输出**: `seed_dict_final_review.tsv` (126个实体，含人工审核标签)

---

### 2.3 实体识别模块

**文件**: `数据增强_词条级匹配.py`

**功能**: 基于种子词典在文本中进行实体匹配

**算法**: 正向最大匹配 (FMM)
- 从句子左侧开始，尝试匹配词典中最长的术语
- 时间复杂度: O(n * L)，n为句子长度，L为最大术语长度

**优化策略**:
- 使用Trie树加速匹配（实际实现中为哈希表+长度排序）
- 重叠实体优先保留较长匹配

**输入**: 清洗后文本 + 种子词典  
**输出**: 标注实体后的文本

---

### 2.4 关系抽取模块

**文件**: `关系抽取方案_v4_最大覆盖.py`

**功能**: 从标注实体的文本中抽取13类医学关系

**核心设计**: 基于规则模板的匹配引擎

```python
# 模板定义示例（药物-治疗-疾病）
{
    'rel_type': '药物-治疗-疾病',
    'head_type': 'Medication',
    'tail_type': 'Disease',
    'patterns': [
        r'(药物A) (可治疗|用于|改善) (疾病B)',
        r'(药物A) (对) (疾病B) (有效)',
    ]
}
```

**13类关系模板分类**:
| 类别 | 关系数 | 模板特点 |
|------|--------|---------|
| 药物相关 | 4类 | 动词驱动（治疗、缓解、导致） |
| 检查相关 | 4类 | 功能驱动（辅助诊断、评估、筛查） |
| 疾病相关 | 3类 | 属性驱动（症状、并发症、治疗） |
| 危险因素 | 1类 | 因果驱动 |
| 诱发关系 | 1类 | 事件驱动 |

**去重策略**:
- 同一对实体同一关系只保留一条
- 优先保留有上下文溯源信息的记录

**输入**: 标注实体后的文本 + 种子词典  
**输出**: `原始三元组_带溯源.json/csv`

---

### 2.5 方向规范化模块

**文件**: `关系抽取结果/疾病统一放头_方向规范化.py`

**功能**: 统一三元组方向，确保疾病实体始终作为头实体

**规范化规则**:
- `并发症-疾病` → `疾病-并发症`
- `症状-疾病` → `疾病-症状`
- 消除`A-相关-B`等双向模糊关系

**目的**: 构建以COPD为中心的有向图，便于Neo4j可视化

**输入**: 原始三元组  
**输出**: `方向规范化_疾病统一在头.tsv` (675条)

---

### 2.6 Neo4j导入文件生成模块

**文件**: `关系抽取结果/生成Neo4j导入.py`

**功能**: 从规范化后的三元组生成Neo4j导入所需的CSV和Cypher脚本

**生成内容**:
1. `nodes.csv`: 126个实体节点（name, type, label_cn）
2. `relations.csv`: 675条关系（head, tail, relation, context）
3. `relations_*.csv`: 13个按类型拆分的关系文件
4. `import.cypher`: 完整的Cypher导入脚本

**编码处理**:
- 统一使用 **UTF-8 无BOM** 编码，避免Neo4j `LOAD CSV` 读取时第一列带BOM头导致属性为空

**数据修正**:
- 硬编码修正已知错误：`CT`→`Examination`，`中成药`→`Medication`

---

### 2.7 端到端演示系统

**文件**: `COPD图谱端到端抽取系统.py`

**功能**: 输入任意COPD临床文本，自动识别实体→抽取关系→更新Neo4j图谱

**系统流程**:
```
用户输入文本
    ↓
[实体识别] 基于种子词典匹配文本中的医学实体
    ↓
[关系抽取] 应用13类规则模板识别实体间关系
    ↓
[冲突检测] 检查新三元组是否与现有知识冲突
    ↓
[图谱更新] 通过py2neo将新节点/关系写入Neo4j
    ↓
[结果展示] 输出抽取结果并给出Neo4j查询语句
```

**配置项**:
```python
NEO4J_URI = "bolt://localhost:7687"
NEO4J_USER = "neo4j"
NEO4J_PASSWORD = "password123"  # 需根据实际修改
```

---

## 三、辅助工具脚本

| 文件 | 功能 |
|------|------|
| `verify_project.py` | 一键验证项目数据完整性 |
| `performance_test.py` | 性能基准测试 |
| `generate_kg_viz.py` | 生成知识图谱可视化图片 |
| `术语归一化_全部中文文献_最终清理.py` | 实体名称标准化去重 |

---

## 四、数据流图

```
原始PDF (50篇)
    ├── PDF清洗_v2.py
    │       └── 清洗文本 (49万字符)
    │               ├── seed_dict_*.py
    │               │       └── 种子词典 (126实体)
    │               │               ├── 数据增强_词条级匹配.py
    │               │               │       └── 标注文本
    │               │               │               ├── 关系抽取方案_v4.py
    │               │               │               │       └── 原始三元组 (1247条)
    │               │               │               │               ├── 人工审核
    │               │               │               │               │       └── 审核后三元组 (981条)
    │               │               │               │               │               ├── 疾病统一放头_方向规范化.py
    │               │               │               │               │               │       └── 规范化三元组 (675条)
    │               │               │               │               │               │               ├── 生成Neo4j导入.py
    │               │               │               │               │               │               │       └── nodes.csv + relations.csv + import.cypher
    │               │               │               │               │               │               │               └── Neo4j Browser (可视化图谱)
    │               │               │               │               │               │               │
    │               │               │               │               │               │               └── COPD图谱端到端抽取系统.py (演示)
```

---

## 五、关键设计决策

### 5.1 为什么选择规则方法而非纯深度学习？

**原因**:
1. **数据限制**: 医学领域标注数据稀缺，675条三元组不足以训练高质量深度学习模型
2. **可解释性**: 规则模板透明可控，便于人工审核和调试
3. **领域特性**: 医学关系表达有较强规律性（如"X可治疗Y"），适合规则捕获
4. **时间成本**: 规则方法开发周期短，适合课程项目时间线

**补充**: 项目仍保留BERT增强实验的接口，未来数据充足时可平滑升级。

### 5.2 如何处理关系方向歧义？

**策略**:
- 医学知识驱动：疾病通常是"被治疗""有症状"的主体，统一作为头实体
- 动词方向决定："A治疗B"中A是药物（头），B是疾病（尾）
- 人工审核兜底：对方向不确定的三元组进行双人审核

### 5.3 如何保证数据质量？

**三层质量控制**:
1. **自动过滤**: 规则模板预筛选，去除明显错误匹配
2. **统计筛选**: PMI+熵值过滤低质量术语
3. **人工审核**: 两名成员独立审核全部1,247条原始三元组，争议讨论后保留675条

---

> 如有疑问，请联系项目组成员或查看 `README.md` 获取运行说明。
