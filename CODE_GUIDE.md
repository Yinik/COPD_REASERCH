# 代码说明

这个文档介绍一下我们项目里各个文件是干嘛的，方便评审老师快速了解代码结构。

---

## 整体流程

整个项目从PDF到知识图谱，大致走下面这几步：

```
50篇PDF医学文献
    ↓  PDF清洗_v2.py（提取文本、去噪声）
清洗后的纯文本（约49万字）
    ↓  seed_dict_*.py（筛选医学术语）
种子词典（126个实体）
    ↓  数据增强_词条级匹配.py（在文本中标注实体）
标注实体的文本
    ↓  关系抽取方案_v4_最大覆盖.py（正则模板匹配关系）
原始三元组（1247条）
    ↓  人工审核 + 疾病统一放头_方向规范化.py
最终三元组（675条）
    ↓  生成Neo4j导入.py（生成CSV和Cypher）
Neo4j知识图谱
    ↓  COPD图谱端到端抽取系统.py（演示系统）
输入文本 → 实时抽取 → 图谱展示
```

---

## 核心文件介绍

### PDF清洗_v2.py

这个文件负责把PDF里的文字弄出来，并且去掉各种噪声。

医学文献的PDF格式比较乱，有的是双栏排版，有的页眉页脚每页都带期刊名和页码，还有参考文献混在正文后面。直接用pdfplumber提取出来的文本根本没法用，所以我们写了一套清洗规则：

- **去页眉页脚**：用正则匹配重复出现的页码/期刊名，把这些行删掉
- **断行合并**：如果一行末尾没有标点（。；！？），就认为是PDF切断的，需要跟下一行拼起来
- **去参考文献**：识别到"参考文献""References"之后的内容全部截掉

**输入**：`原始文献数据/*.pdf`  
**输出**：`data/清洗后的文本数据/*_cleaned.txt`

---

### seed_dict_select.py / seed_dict_filter.py / seed_dict_pmi_entropy.py

这三个文件是一套，用来从清洗后的文本里筛选出高质量的医学术语，构建种子词典。

**第一步**（`seed_dict_select.py`）：用N-gram和规则匹配生成候选术语。这一步会生成大量噪声，因为好多高频词并不是医学术语。

**第二步**（`seed_dict_filter.py`）：过滤掉明显不是术语的词，比如停用词、单字词、纯英文缩写等。

**第三步**（`seed_dict_pmi_entropy.py`）：用统计方法给候选术语打分。我们算了两个指标：
- **PMI（点互信息）**：看一个多字词内部是不是够"黏"。比如"支气管"三个字经常一起出现，PMI就高；"的支气管"这种组合PMI就低。
- **信息熵**：看一个词的左右搭配丰不丰富。如果一个词左边只能接"的"，右边只能接"了"，那它大概率是通用词，不是专业术语，应该过滤掉。

最后输出一批候选术语，然后我们再人工审核一遍，确认是不是医学实体，分到哪个类别。

**输出**：`seed_dict_final_review.tsv`（126个实体，带人工标签）

---

### 数据增强_词条级匹配.py

这个文件用种子词典在文本里找实体。算法很简单，就是**正向最大匹配**（FMM）：

从句子左边开始，尝试匹配词典中最长的术语。比如句子是"慢性阻塞性肺疾病的症状包括咳嗽"，先匹配"慢性阻塞性肺疾病"（8个字），匹配上了就继续往后走。

实现上没有用Trie树（懒，而且数据量不大），用的是哈希表+按长度排序，效果差不多。

---

### 关系抽取方案_v4_最大覆盖.py

这个是最核心的文件之一，负责从标注了实体的文本里抽关系。

我们给13种关系各写了几条正则模板。比如"药物-治疗-疾病"这种关系，模板大概长这样：

```python
{
    'rel_type': '药物-治疗-疾病',
    'head_type': 'Medication',
    'tail_type': 'Disease',
    'patterns': [
        r'(药物A) (可治疗|用于|改善) (疾病B)',
        r'(药物A) (对) (疾病B) (有效)',
    ]
}
```

文本里如果出现了词典里的药物实体和疾病实体，并且中间的句子匹配上了这些正则，就抽一条三元组出来。

**去重策略**：同一对实体同一关系只保留一条，优先保留有上下文溯源信息的（就是记下这条三元组是从哪篇文献、哪句话里抽出来的，方便后面审核的时候核对原文）。

**为什么是v4**：因为前面试了3版，第1版只写了4类关系，覆盖太少；第2版加了共现匹配，噪声太大；第3版优化了模板但还是有方向问题。到第4版才算比较稳定，所以叫"v4_最大覆盖"。

**输出**：`原始三元组_带溯源.json/csv`（1247条）

---

### 疾病统一放头_方向规范化.py

这个文件做两件事：

1. **统一方向**：比如原始抽取里有"并发症-疾病"也有"疾病-并发症"，我们统一改成"疾病-并发症"，让疾病始终作为头实体。这样Neo4j里画出来的图谱才清晰，COPD才能作为中心节点向外辐射。

2. **去掉模糊关系**：把"相关"这种双向模糊的关系删掉，因为图谱里这种关系画了等于没画，看不出来谁影响谁。

**输出**：`方向规范化_疾病统一在头.tsv`（675条）

---

### 生成Neo4j导入.py

从规范化后的三元组生成Neo4j能直接导入的文件。

生成四个东西：
1. `nodes.csv`：126个实体（name, type, label_cn三列）
2. `relations.csv`：675条关系（head, tail, relation, context四列）
3. `relations_*.csv`：按关系类型拆分的13个CSV，方便分批导入
4. `import.cypher`：Cypher导入脚本，直接在Neo4j Browser里执行就行

**特别注意编码**：必须用**UTF-8无BOM**，不能用`utf-8-sig`。我们踩过这个坑——一开始用`utf-8-sig`生成的CSV，Neo4j的`LOAD CSV`读出来第一列name全是null，排查了半天才发现是BOM头的问题。

**还修了两个数据错误**：`CT`被错标成了其他类型，硬编码改成了`Examination`；`中成药`被错标成了其他类型，改成了`Medication`。

---

### COPD图谱端到端抽取系统.py

演示系统。输入一段COPD临床文本，自动：
1. 用种子词典匹配出文本里的医学实体
2. 用13类规则模板抽关系
3. 检查新抽出来的三元组跟已有知识冲不冲突
4. 通过py2neo写到Neo4j里
5. 输出结果，并给几条Neo4j查询语句方便查看

**需要改配置**：
```python
NEO4J_URI = "bolt://localhost:7687"
NEO4J_USER = "neo4j"
NEO4J_PASSWORD = "password123"  # 改成你自己的密码
```

如果本地没装Neo4j，这个文件跑不了（会报连接错误）。但数据处理和关系抽取部分是可以独立运行的。

---

## 辅助工具

| 文件 | 干嘛用的 |
|------|---------|
| `verify_project.py` | 一键检查数据完整性，5项验证 |
| `performance_test.py` | 测加载速度和查询性能 |
| `generate_kg_viz.py` | 不启动Neo4j也能生成图谱图片 |
| `术语归一化_全部中文文献_最终清理.py` | 实体名称去重和标准化 |

---

## 一些设计上的考虑

### 为什么用规则而不是深度学习？

其实我们也试了BERT（`bert-base-chinese`），但问题是：
1. 我们自己标的675条数据，训深度学习模型根本不够
2. 医学文本的关系表达很有规律，"X可治疗Y"这种模式正则就能抓
3. 规则抽错了能直接改模板，BERT抽错了都不知道怎么调

所以目前以规则为主，BERT作为一个预留的优化方向。

### 关系方向怎么确定的？

主要靠医学常识：疾病通常是被治疗、有症状的主体，所以统一把疾病放在头实体的位置。比如"COPD的症状是呼吸困难"，规范化成"COPD → 症状 → 呼吸困难"。有些方向模棱两可的，就两个人一起审，讨论决定。

### 数据质量怎么保证？

三层把关：
1. 规则模板预筛一遍，去掉明显离谱的匹配
2. PMI+熵值筛掉低质量术语
3. 最后人工审核，两个人独立看全部1247条，有争议的再讨论

---

有问题可以看看 `README.md`，里面有运行说明。或者看 `TEST_REPORT.md`，里面有详细的测试结果。
