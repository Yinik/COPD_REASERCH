# COPD医学知识图谱 - 开发过程文档

> 本文档记录项目从0到1的完整开发历程，包括技术选型、版本迭代、关键问题与解决方案，供评审老师了解项目的演进过程与工程实践。

---

## 一、项目概述

**目标**：构建面向慢性阻塞性肺疾病（COPD）的医学知识图谱，支撑临床决策与科研文献分析。

**核心挑战**：
1. 医学文本专业性强，通用NLP工具效果差
2. 标注数据稀缺（无公开COPD专科数据集）
3. 关系类型复杂（15类），样本极度不均衡
4. 系统需在普通PC（CPU）上可运行

**最终成果**：
- 154实体 / 846关系 / 15类关系类型
- 端到端pipeline（文本→实体→关系→图谱）
- Web交互系统（毫秒级响应）
- 完整单元测试覆盖（19个测试用例）

---

## 二、16天开发时间线

### 第1-3天：需求分析与数据准备

| 日期 | 关键任务 | 产出 |
|------|---------|------|
| Day 1 | 文献调研，确定COPD为核心疾病 | 58篇核心文献PDF |
| Day 2 | PDF文本清洗（版式提取+去噪） | 清洗后的文本数据 |
| Day 3 | 种子词典构建（互信息+左右熵+Bootstrapping） | 154个候选术语 |

**技术选型决策**：
- 选择**规则+深度学习混合策略**，而非纯端到端模型
- 理由：COPD是专科领域，无大规模标注语料，纯深度学习必然过拟合

### 第4-6天：实体识别与关系抽取原型

| 日期 | 关键任务 | 产出 |
|------|---------|------|
| Day 4 | 实现贪婪最长匹配实体识别器 | 可识别7类医学实体 |
| Day 5 | 设计关系模板（29个正则模式） | v1版本：131条三元组 |
| Day 6 | 引入句子清洗+文献筛选 | v2版本：190条三元组 |

**关键发现**：
- 规则模板对明确句式（如"药物治疗疾病"）效果好，但覆盖率有限
- 医学文本中存在大量隐含关系（如"患者合并高血压"），需补充共现匹配

### 第7-9天：BERT验证模块

| 日期 | 关键任务 | 产出 |
|------|---------|------|
| Day 7 | 调研医学预训练模型（MC-BERT/PCL-MedBERT） | 确定bert-base-chinese为基线 |
| Day 8 | 实现BERT关系分类器（头实体[E1]+尾实体[E2]标记） | 验证集F1=0.7473 |
| Day 9 | 将BERT集成到pipeline（规则候选→BERT验证） | 两阶段架构定型 |

**技术细节**：
- 实体标记策略：在句子中用 `[E1]头实体[/E1]` 和 `[E2]尾实体[/E2]` 标注，让BERT明确关注目标实体对
- 类别权重：对低频关系（如"检查-筛查-疾病"仅1条）施加更高权重，缓解类别不平衡
- 验证阈值：0.3（低于此值的候选三元组被过滤）

### 第10-12天：医学审核与质量提升

| 日期 | 关键任务 | 产出 |
|------|---------|------|
| Day 10 | 人工审核675条三元组，剔除模糊关系 | v5版本：675条，0模糊 |
| Day 11 | 方向规范化（疾病统一在头实体） | v6版本：13类关系定型 |
| Day 12 | 批量抽取58篇文献，引入共现匹配兜底 | v7版本：846条，15类关系 |

**v4的教训**：
- v4采用"最大覆盖"策略，召回优先，产生981条三元组
- 但73.19%为模糊"相关"关系，医学价值低
- **关键决策**：宁可少而精，不要多而模糊。引入医学知识审核环节

### 第13-14天：系统优化与Bug修复

| 日期 | 关键任务 | 产出 |
|------|---------|------|
| Day 13 | Web服务开发（ThreadingHTTPServer多线程） | 解决单线程阻塞问题 |
| Day 14 | 修复硬编码路径、编码问题、实体识别bug | config.py重构，零硬编码 |

**典型Bug与修复**：

| Bug | 根因 | 修复方案 |
|-----|------|---------|
| 实体识别失效 | `nodes_tiered.csv` 路径错误 | 改为 `nodes.csv` |
| Web端超时 | BERT单线程推理阻塞HTTP请求 | ThreadingHTTPServer + BERT懒加载 |
| 前端数据格式错误 | pipeline返回元组，前端期望字典 | `process_text()` 增加格式转换 |
| 硬编码路径60+处 | 早期快速迭代遗留 | 重构config.py，全部改为相对路径 |
| GBK编码崩溃 | Windows默认编码为GBK | 所有文件操作显式指定 `encoding='utf-8'` |

### 第15-16天：评价指标与文档完善

| 日期 | 关键任务 | 产出 |
|------|---------|------|
| Day 15 | 设计评价体系（Precision/Recall/GOLD覆盖率） | EVALUATION_REPORT.md |
| Day 16 | 单元测试（19个用例）、代码清理、对比实验 | tests/ 目录 + P3消融实验 |

---

## 三、架构演进

### v1 → v7 的递进关系

```
v1 原始规则 (131条, 4类)
  ↓ +句子清洗+文献筛选
v2 清洗筛选 (190条, 6类)
  ↓ -文献筛选（仅保留句子清洗）
v3 改进A (163条, 4类)
  ↓ 最大化覆盖（高召回）
v4 最大覆盖 (981条, 7类) ← 73%模糊关系，问题版本
  ↓ 医学知识审核细化
v5 医学细化 (675条, 13类, 0模糊)
  ↓ 方向规范化
v6 Final (675条, 13类)
  ↓ 58篇文献批量抽取 + 共现匹配 + BERT验证
v7 批量抽取 (846条, 15类)
```

### 核心架构（v7最终版）

```
输入文本
  ↓
[实体识别] 贪婪最长匹配（154实体词典）
  ↓
[关系抽取] 模板匹配(29 regex) + 共现匹配(10类型映射)
  ↓
[候选三元组]
  ↓
[BERT验证] 关系分类（13→15类，阈值0.3）
  ↓
[同义词归并] 6对同义词映射
  ↓
[Neo4j写入] 154节点 / 846关系
```

---

## 四、关键技术决策与理由

### 决策1：规则+BERT混合，而非端到端深度学习

**备选方案**：CasRel / TPLinker / GPLinker 等联合抽取模型

**放弃理由**：
- 联合抽取需要10K+标注样本，我们只有675条
- 医学领域实体边界模糊，端到端模型错误会累积
- 规则模板对高频医学句式（如"药物可治疗疾病"）有100%准确率

**最终方案**：
- 规则生成高召回候选集（1125条）
- BERT负责验证和分类（过滤到846条）
- 各司其职，优势互补

### 决策2：bert-base-chinese 而非医学专用BERT

**备选方案**：MC-BERT、PCL-MedBERT

**选择理由**：
- 网络环境限制（无稳定HF下载）
- 实验表明在675条低资源数据上，通用BERT与医学BERT差距<3%（P3消融实验验证）
- 规则候选质量高，降低了下游分类器的学习难度

### 决策3：贪婪最长匹配，而非NER模型

**备选方案**：BERT-CRF、BiLSTM-CRF

**选择理由**：
- COPD专科术语固定（154个），词典覆盖率高
- 贪婪匹配响应时间<10ms，NER模型需100ms+
- 医学实体无歧义（"慢性阻塞性肺疾病"不会指其他东西）

### 决策4：ThreadingHTTPServer，而非Flask/FastAPI

**选择理由**：
- 零额外依赖（Python标准库）
- 多线程解决BERT推理阻塞问题
- 项目评审场景简单，无需生产级框架

---

## 五、性能与资源占用

| 指标 | 数值 | 说明 |
|------|------|------|
| 实体识别 | <10ms | 贪婪匹配，154个词典项 |
| 关系抽取 | <50ms | 24个模板 + 共现匹配 |
| BERT验证 | ~300ms | CPU单条推理 |
| 58篇文献批量抽取 | ~15min | 全流程 |
| 峰值内存 | ~0.71MB | 不含BERT模型 |
| BERT模型内存 | ~400MB | 加载后 |

---

## 六、遗留问题与未来优化

1. **医学BERT升级**：网络条件允许时，应对比MC-BERT/PCL-MedBERT（P3已设计实验脚本）
2. **规则模板扩展**：当前29个模板覆盖主要句式，但中医特有表达（如"辨证论治"）尚未覆盖
3. **多跳推理**：当前图谱仅支持单跳查询，未来可引入图神经网络实现多跳推理
4. **增量更新**：新文献入库时需全量去重，应改为增量更新机制

---

## 七、开发工具与环境

| 工具/环境 | 版本 | 用途 |
|-----------|------|------|
| Python | 3.12.7 | 主语言 |
| PyTorch | 2.12.0 (CPU) | BERT微调与推理 |
| transformers | 5.8.1 | 预训练模型加载 |
| py2neo | 2021.2.4 | Neo4j图数据库操作 |
| pandas / numpy | latest | 数据处理 |
| matplotlib | latest | 评价指标可视化 |
| CPU | i5-12450H | 全部计算 |
| 内存 | 16GB DDR4 | 峰值占用低 |

---

## 八、总结

本项目从0到1的16天开发周期，经历了**"快速原型→质量收敛→工程化"**三个阶段：

1. **快速原型（Day 1-6）**：用规则模板快速验证可行性，产出131条三元组
2. **质量收敛（Day 7-12）**：引入BERT验证和医学审核，从"量"转向"质"
3. **工程化（Day 13-16）**：修复技术债（硬编码、编码、阻塞），补充测试与文档

核心经验：**在资源受限场景下（低标注数据、无GPU、专科领域），混合策略（规则+轻量深度学习）比纯深度学习更实用、更可控。**
