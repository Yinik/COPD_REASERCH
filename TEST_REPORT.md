# 测试记录

这是我们做的一些测试，用来验证项目的数据和代码是不是靠谱。

**项目**: 基于COPD医学文本的实体关系抽取与知识图谱构建  
**测试时间**: 2025年5月  
**测试人**: 尹鑫、苟玉莲  
**环境**: Windows 11 + Python 3.12.7，普通笔记本（16GB内存）

---

## 测了哪些东西

我们主要测了五个方面：
1. 数据对不对（三元组数量、实体数量、文件齐不齐全）
2. PDF清洗效果好不好
3. 关系抽取准不准
4. Neo4j能不能正常导入和显示
5. 运行速度快不快

---

## 数据完整性测试

写了个脚本 `verify_project.py`，一键检查所有核心数据。

**测了什么**：
- 三元组是不是675条、13类关系 —— 结果是对的
- 126个实体的类型分布对不对 ——  Diseases 28个、Symptoms 25个、Medications 34个...全部匹配
- Neo4j导入文件齐不齐全 —— nodes.csv、relations.csv、import.cypher、README.md 都在
- 图谱连通性 —— COPD能走到115个实体，总共126个，连通率91.3%。剩下11个走不到的是一些孤立的中药名，这个问题我们已知，不影响主要展示
- 有没有"相关"这种模糊关系 —— 检查通过，没有发现

**结果**: 所有检查项都通过了。

---

## PDF清洗测试

随机选了5篇文献，人工比对清洗前后的文本质量。

**样本**: 
- `1_高剂量N-乙酰半胱氨酸...`
- `5_2025年GOLD...`
- `28_POCKET-GUIDE...`
- `40_GOLD-2024...`
- `50_慢性阻塞性肺疾病稳定期...`

**检查内容**:
- 页眉页脚去掉了吗？ —— 基本都去掉了，偶尔有一两处残留
- 断行合并了吗？ —— 合并了，语义基本完整
- 参考文献截断了吗？ —— 截了
- 有乱码吗？ —— UTF-8编码，没有乱码

**结果**: 清洗效果可以，满足后续处理需求。个别复杂排版的PDF（比如带大量表格的）处理得不是特别干净，但正文部分没问题。

---

## 关系抽取准确性测试

我们抽样检查了45条三元组，人工回去核对原文，看看抽得对不对。

**抽样方法**: 每类关系随机抽3-5条

**抽查结果**:

大部分关系类型都抽得很准，比如"药物-治疗-疾病"、"疾病-症状"这些常见的，5条里5条全对。"诱发-急性加重"这类稍微复杂一点的，5条里有1条方向有点问题（把"COPD急性加重"当成了头实体而不是尾实体），准确率80%。

总体算下来，45条里对了44条，准确率97.8%。达到了我们预期的95%以上。

---

## Neo4j导入测试

按照我们自己写的 `Neo4j导入教程.md` 一步一步操作，看看能不能跑通。

**步骤**:
1. 清空Neo4j数据库
2. 把CSV文件复制到import目录
3. 在Neo4j Browser里执行import.cypher里的语句
4. 查一下节点数和关系数对不对

**结果**:
- 导入节点：126个 ✅
- 导入关系：675条 ✅
- 创建索引：1个 ✅
- 给节点加类型标签：126个 ✅
- 执行COPD辐射图查询：能正常显示网络图 ✅
- 药物子图查询：也能正常显示 ✅

**遇到的小问题**：一开始CSV用的是`utf-8-sig`编码，导入后节点属性全是null，排查了半天发现是BOM头的问题。改成`utf-8`（无BOM）之后就好了。这个坑我们在生成脚本里已经修复了。

---

## 性能测试

写了个 `performance_test.py` 测了一下速度。

**测试结果**:

- 加载675条三元组：6毫秒
- 构建126个实体的索引：不到1毫秒
- 处理10篇文献的实体识别：12毫秒，大概每秒能处理2000万字符
- 按关系类型过滤：不到1毫秒
- 查COPD的邻居：不到1毫秒
- BFS遍历2跳内的节点：不到1毫秒
- 内存占用：三元组+实体总共0.71MB

**结论**: 速度完全够用了，普通笔记本跑起来毫无压力。毕竟数据量不大（675条关系），Neo4j本地查询也是毫秒级响应。

---

## 兼容性测试

| 环境 | 结果 |
|------|------|
| Python 3.12 + Windows 11（尹鑫电脑） | 正常 |
| Python 3.10 + Windows 10（苟玉莲电脑） | 正常 |
| Neo4j 5.x Desktop | 正常 |
| 不装Neo4j，只跑Python脚本 | 也能跑（跳过图谱可视化部分） |

---

## 还发现哪些问题

1. **图谱不是完全连通的**。126个实体里有11个从COPD出发走不到，比如一些孤立的中药名称。这个我们接受，毕竟不是所有实体都跟COPD直接相关。

2. **端到端系统需要手动配Neo4j连接**。如果本地没装Neo4j或者密码不对，系统会报错。这个我们在README里写清楚了配置方法。

3. **BERT那部分没怎么深入**。主要是标注数据只有675条，训BERT效果不稳定，所以目前还是以规则为主。

---

## 总体结论

数据是对的，代码能跑，速度够快，Neo4j能正常显示。项目达到中期检查的基本要求了，接下来主要是做PPT和演示材料。
