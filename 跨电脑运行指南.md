# 跨电脑运行指南

## 概述

本项目已通过 `config.py` 统一管理所有路径，换电脑运行时**只需修改一处路径**即可。

---

## 第一步：复制项目文件

把 `101实验专题` 整个文件夹复制到新电脑，**以下文件/文件夹必须包含**：

| 必须复制 | 说明 |
|----------|------|
| `*.py` 所有代码文件 | 已去除硬编码路径 |
| `config_template.py` | 用于生成 config.py |
| `requirements_core.txt` | 依赖清单 |
| `原始文献数据/` | PDF 文献 |
| `data/` 数据文件夹 | 清洗后的文本 |
| `关系抽取结果/` | 三元组数据 |
| `种子词典构建结果/` | 种子词典 |
| `bert_relation_output/` | **BERT 模型文件**（best_model.pt） |

| **不需要复制** | 说明 |
|----------------|------|
| `.venv/` | 虚拟环境不可移植，需重建 |
| `.git/` | Git 仓库（可保留） |
| `__pycache__/` | Python 缓存 |
| `output/` `result/` | 输出目录可自动创建 |

---

## 第二步：安装 Python 环境

### 1. 安装 Python（如果还没有）
- 推荐 Python 3.12
- 官网: https://www.python.org/downloads/
- **安装时勾选 "Add Python to PATH"**

### 2. 创建虚拟环境

打开 CMD 或 PowerShell，进入项目目录：

```bash
cd I:\101实验专题        # 改成你的实际路径
python -m venv .venv
```

### 3. 激活虚拟环境

```bash
# Windows CMD
.venv\Scripts\activate.bat

# Windows PowerShell
.venv\Scripts\activate.ps1
```

### 4. 安装依赖

```bash
pip install -r requirements_core.txt
```

> 如果安装失败，换用完整版：`pip install -r requirements.txt`

---

## 第三步：创建 config.py

```bash
copy config_template.py config.py
```

用文本编辑器打开 `config.py`，**只改这一行**：

```python
# 原来
BASE_DIR = Path(r"I:\101实验专题")

# 改成新电脑的实际路径，例如：
BASE_DIR = Path(r"D:\我的项目\101实验专题")
# 或 Linux/Mac:
# BASE_DIR = Path("/home/username/101实验专题")
```

**保存即可，其他路径会自动基于 BASE_DIR 计算。**

---

## 第四步：复制 BERT Tokenizer 缓存（离线运行）

如果新电脑**不能联网**，需要手动复制 tokenizer 文件：

**从旧电脑复制到新机：**

```
旧电脑: C:\Users\<用户名>\.cache\huggingface\hub\models--bert-base-chinese
新电脑: C:\Users\<新用户名>\.cache\huggingface\hub\models--bert-base-chinese
```

如果新机**能联网**，第一次运行时会自动下载（约 400MB），**无需手动复制**。

---

## 第五步：验证运行

### 测试 BERT 关系分类器

```bash
# 激活环境
.venv\Scripts\activate

# 设置离线模式（如果不能联网）
set TRANSFORMERS_OFFLINE=1

# 测试预测
python bert_relation_classifier.py --mode predict --sentence "吸烟是慢性阻塞性肺疾病的重要危险因素" --head "吸烟" --tail "慢性阻塞性肺疾病"
```

如果输出预测结果（如 `风险因素-疾病`），说明环境配置成功。

---

## 第六步：批量预测

```bash
# 对已有三元组做 BERT 重新预测
python bert_batch_predict.py --input "关系抽取结果\v4_最大覆盖版_原始三元组_带溯源.json"
```

结果保存到 `bert_relation_output\bert_predictions.tsv`

---

## 常见问题

### Q: `ModuleNotFoundError: No module named 'config'`
**A**: 确保在项目根目录运行代码（`cd I:\101实验专题`），且 `config.py` 存在于根目录。

### Q: `OSError: Error no file named pytorch_model.bin`
**A**: BERT tokenizer 缓存缺失。联网运行一次即可自动下载，或按第四步手动复制。

### Q: 中文路径乱码
**A**: 确保文件编码为 UTF-8。PowerShell 用户可在运行前执行：
```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
```

---

## 总结

| 步骤 | 操作 |
|------|------|
| 1 | 复制项目文件夹 |
| 2 | `python -m venv .venv` |
| 3 | `pip install -r requirements_core.txt` |
| 4 | 改 `config.py` 里的 `BASE_DIR` |
| 5 | 复制 BERT 缓存（离线时） |
| 6 | `python bert_relation_classifier.py --mode predict ...` |

**核心原则：换电脑 = 复制文件夹 + 改 BASE_DIR + 重装依赖。**
